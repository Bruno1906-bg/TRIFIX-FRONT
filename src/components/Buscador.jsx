import './Buscador.css'

function Buscador() {
  return (
    <input
      type="text"
      placeholder="Buscar reporte por estatus, área, ID, usuario, fecha, prioridad..."
      className="buscador"
    />
  )
}

export default Buscador
